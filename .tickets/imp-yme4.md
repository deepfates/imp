---
id: imp-yme4
status: in_progress
deps: []
links: []
created: 2026-07-25T16:21:13Z
type: epic
priority: 0
assignee: deepfates
tags: [dspy, parity, optimizers, gepa, optimize-anything, product]
---
# Make Imp's advertised DSPy semantics real

Carry Imp from its large implemented surface and conflicting evidence systems to honest semantic parity with DSPy and the useful surrounding ecosystem, while keeping Elixir-native equivalents where Python mechanics are incidental. Dashboard work is supporting infrastructure, not the endpoint; fix or remove facades by exercising the actual behavior users depend on.

## Acceptance Criteria

For every public surface advertised in the README and Imp-for-DSPy mapping, the repository identifies the upstream semantic contract and demonstrates either reproducible behavioral/differential parity or an explicit superior BEAM-native equivalent. Core declaration, execution, adapters, modules, evaluation, retrieval/tool/agent composition, persistence/operation, and optimizer families are usable from an ordinary consumer project. GEPA, Optimize Anything, MIPROv2/SIMBA/COPRO, bootstrap/random-search families, and any other advertised optimizer have meaningful held-out task outcomes rather than structural smoke alone; matched comparisons state model/provider/cost/seed/splits and disagreements. One killer end-to-end optimization example shows material improvement on unseen data and leaves an inspectable reusable program artifact. Generated conformance, claims, docs, and executable dashboards agree from a clean checkout. Missing upstream features stay visibly open; package or release readiness alone cannot close this epic.

## Notes

**2026-07-25T16:27:40Z**

2026-07-25 deterministic-truth slice: the committed-evidence dashboard at current HEAD computes 10 proven / 26 blocked / 19 informational, profile_ready=false. Restored docs/EVIDENCE.md to those honest counts and moved the venv-free reconciliation regression into the normal test suite so fast.check catches future prose drift; ephemeral lane directories remain replaced by fresh empty temp paths in the test, so local tmp state cannot promote claims. No evidence was made proven. Focused Mix execution was unavailable because the sandbox forbids Mix.PubSub TCP sockets and the escalation was aborted; git diff --check and the current dashboard JSON were verified. Epic remains open for semantic parity.

**2026-07-25T16:28:25Z**

Principal verification after f1e05c8: mix test test/evidence_reconciliation_test.exs passed 1 test, 0 failures in 1.2s under the ordinary exclusion set, confirming the regression now runs despite evidence_infrastructure being excluded.

**2026-07-25T18:31:00Z**

Principal semantic-parity checkpoint. Recovered and retained the existing authority hierarchy instead of adding another matrix: claims.json is the claim ledger; authorities.json pins upstream identity; reproductions.json maps surfaces/protocols/admitted tiers; generated CONFORMANCE.md and maintainer reproductions/authority pages are projections; COVERAGE_MATRIX, PARITY_VALIDATION_PROGRAM, BENCHMARK_TRUTH, and specialized fidelity notes add human context but cannot override the machine chain. Commits 927cd8e, 1f653ad, 7e64feb, e358ee8, 0169333, and ec250e9 respectively repaired immutable artifact admission, independently admitted the narrow GEPA v0.1.4 structural contract, made `with_lm`/`with_demos` traverse complete executable graphs, preserved typed SIMBA reflection inputs, rejected no-op generic instruction optimization, and implemented native DSPy 3.2.1 InferRules induction with an exact provider-free differential.

Optimize Anything C3 audit found that immutable artifact 58ff84ac selected candidates and computed final scores on the same development set. The artifact remains exact T2 live-execution evidence but its C3 claim is now a telos target. Schema v2 implements pairwise-distinct train/selection/test datasets for code, agent configuration, and scheduling; passes only on multi-seed test lift; selects the displayed representative by selection score; and requires the pure provenance/accounting validator before the dashboard can accept full evidence. Focused optimizer/evidence execution passed 46 tests with zero failures, including the stubbed three-family live campaign; `mix reproduction.check` passed. The full default suite then passed 53 doctests, 9 properties, and 2,268 tests with zero failures and 11 skips. No schema-v2 provider campaign has been run or admitted, so Optimize Anything effectiveness remains open. GEPA 9dbefc4 remains preserved and independently verified only as structural conformance, not effectiveness. The epic remains open for held-out optimizer outcomes, remaining public facades, and broad package-consumer exercise.

**2026-07-25T18:41:30Z**

LabeledFewShot user-semantics slice: restored DSPy 3.2.1's sampled `k=16`, seed-zero defaults; added the ordered `sample: false` path and explicit integer `seed:`; clears stale demos; and applies separate no-replacement draws from one advancing stream to every optimizer-exposed predictor. Imp deliberately uses serializable BEAM optimizer RNG state rather than Python `random.Random`, so exact equal-seed subset ordering is excluded while determinism, no replacement, traversal, and ordered selection are preserved. The tutorial and its runner now explicitly select `sample: false`, which names the ordered behavior used by the immutable live artifact instead of relying on the former Imp default. Conformance classifies the family as an Elixir-native equivalent and keeps held-out family effectiveness open. Focused source/docs/conformance/package execution passed 61 tests with zero failures; the existing provider-free optimizer differential passed 10/10 rows and directly matched the explicit ordered LabeledFewShot comparison. Exact Python RNG-sequence parity and general LabeledFewShot effectiveness remain unclaimed.

**2026-07-25T19:10:40Z**

Broad consumer-semantics checkpoint through `8809fdf`. Runtime repairs after the prior checkpoint now include InferRules evaluation-error enforcement, recursive Ensemble demo rebinding, native categorical-TPE exploration, consumer-usable SFT/GRPO training validation, Avatar failed-candidate rejection, and a supported Optimize Anything best-candidate accessor. Documentation now names InferRules' whole-loop deviations and removes whole-port and unmeasured BEAM-superiority language. A clean-room package consumer constructs and executes LabeledFewShot, BootstrapFewShot, RandomSearch, KNNFewShot, COPRO, MIPROv2, SIMBA, GEPA, and InferRules through public paths; these deterministic Static-LM programs establish callable lifecycle and candidate application, not effectiveness.

Evidence remains separated by kind. Independently executed upstream behavior consists of the preserved 15-case GEPA v0.1.4 structural differential, narrow pinned DSPy 3.2.1 differentials for the few-shot/instruction families, exact InferRules formatting/update observations plus a controlled upstream loop probe, and freshly recaptured AvatarOptimizer/mmGRPO source-bound C1 observations. Imp unit and package-consumer behavior is broader than those upstream probes. Structural fixtures cover deterministic candidate/trace/lifecycle paths. No new family-wide held-out optimization result was established in this slice; the Optimize Anything schema-v2 multi-seed train/selection/test campaign and the killer reusable-program example remain open. InferRules is not whole-loop equivalent: Imp protects the baseline, isolates immutable candidates instead of sharing mutable signature classes, records/skips proposal and evaluation failures instead of aborting, resolves the rule LM explicitly, uses sequential BEAM rollout IDs, and does not retry oversized rule prompts by dropping examples.

The existing hierarchy remains authoritative: this ticket states the endpoint; `benchmarks/claims.json` states public claims; `benchmarks/authorities.json` pins upstream identities; `benchmarks/reproductions.json` maps surfaces, protocols, and admitted evidence; `mix benchmark.dashboard` computes status; generated conformance/maintainer pages are projections; older matrices and fidelity notes are context only. Fresh AvatarOptimizer and mmGRPO execution restored strict source-bound registry agreement without relaxing validation. The dashboard is again 10 proven / 25 blocked / 19 informational and profile-ready remains false.

Churn from `927cd8e^` through `8809fdf`, counting additions plus deletions, is 825 library/runtime lines (16.1%), 2,624 admission/benchmark lines (51.1%), and 1,683 test/doc/support lines (32.8%). Immutable admitted receipts account for 1,957 of the benchmark lines; excluding them, the split is 825 runtime (26.0%), 667 evidence infrastructure (21.0%), and 1,683 tests/docs/support (53.0%). This records the receipt-heavy diff without treating receipt volume as product progress. No head-to-head result currently supports a BEAM-native superiority claim.

Verification: the default suite passed 53 doctests, 9 properties, and 2,283 tests with zero failures and 11 skips; the exact AvatarOptimizer/mmGRPO evidence tests passed 7/7; the strict reproduction registry/check and dashboard reconciliation passed; `mix package.check` passed 13/13 plus fresh unpacked tutorial, persistence/tamper, and release probes. Highest remaining facades are held-out optimizer effectiveness across the advertised families, Optimize Anything's schema-v2 live campaign, provider-backed BootstrapFinetune/GRPO quality and continuation behavior, BetterTogether's sub-10-row validation/selection semantics and real weight step, and full InferRules loop comparison. The epic remains open.

**2026-07-25T20:06:09Z**

Integrated optimizer-semantics checkpoint through `a695f3f`, followed by a generated-conformance alignment correction. Ordinary provider-free consumers now execute the advertised prompt/search families through public package paths; `SignatureOptimizer` and `Ensemble` are also constructed and called from the unpacked artifact. `BetterTogether` now reserves validation data for every automatic split with at least two examples, selects the scored prefix instead of silently returning the latest prefix on small datasets, and can execute a real public `BootstrapFinetune` weight step through an injected trainer before rebinding the trained model. This proves composition and lifecycle mechanics with a planted local trainer, not optimizer effectiveness.

`InferRules` now exercises implicit splitting, multi-predictor induction/evaluation, retry-by-dropping-one-example, and report placement against an independently executed DSPy 3.2.1 loop probe. It remains a deliberate safer adaptation rather than whole-loop equivalence: Imp protects the baseline and isolates candidate signatures, retries only the typed context-window error, uses stable sequential rollout IDs through retries, and records exhausted/failed candidates; upstream also retries broader exceptions, generates fresh random IDs, raises on exhaustion, and aliases mutable signature classes so later proposals can mutate earlier scored candidates. No held-out InferRules lift is claimed.

Fast-Slow now enforces logical operation budgets, persists ordered intent/confirmation/retry/exhaustion events, and accepts valid early terminal partial lookahead while preserving paper-ordered T/K/G Algorithm-1 orchestration. The user-facing claim has been narrowed everywhere to an external slow-weight/CISPO-shaped handoff. Imp does not implement CISPO loss, clipping, gradients, optimizer updates, or resulting model weights, and the official code authority is still unavailable. Concurrent rollout throughput and provider effectiveness also remain open.

The repaired BetterTogether runtime invalidated its old source-bound C1 receipt. A clean pinned DSPy 3.2.1 recapture matched the same three admitted prefix/tie observations with unchanged exclusions, and the new immutable receipt was admitted without widening the claim. The canonical authority/reproduction/claim chain, generated maintainer pages, and conformance report agree. The generated Optimize Anything conformance section now also names the already-canonical open schema-v2 target: at least three live provider-backed seeds across code, agent configuration, and scheduling with distinct train, selection, and untouched test sets. No campaign was run and no claim became proven.

Broad execution found no integrated behavior regression: upstream fidelity and authority checks passed; public-surface tests passed 41/41; the deterministic Fast-Slow campaign passed; package contracts passed 13/13 plus the unpacked provider-free tutorial, clean-room persistence/tamper checks, and a production release probe; integration passed 9/9; the exact BetterTogether receipt/registry/reconciliation slice passed 16/16; and the default suite passed 53 doctests, 9 properties, and 2,291 tests with zero failures and 11 skips. The dashboard remains 10 proven / 25 blocked / 19 informational, profile-ready false. No provider spend occurred.

Highest-value remaining work is outcome evidence, not more structural taxonomy: multi-seed untouched held-out effectiveness under matched budgets for BootstrapFewShot/RandomSearch/COPRO/MIPROv2/SIMBA/InferRules/GEPA and the composition/weight families; real operational and paper-family GEPA execution beyond the preserved v0.1.4 structural differential; the Optimize Anything schema-v2 live campaign and matched upstream portfolio; provider-backed BootstrapFinetune/GRPO/BetterTogether completion and quality; an externally authorized CISPO implementation or provider handoff with content-bound model artifacts; and the epic's killer end-to-end reusable optimized program. No head-to-head result supports a BEAM-native superiority claim.

For this five-commit slice (`4f559d9` through `a695f3f`), additions plus deletions are 315 library/runtime lines (19.3%), 687 benchmark/admission lines (42.2%), and 627 test/doc/support lines (38.5%). The refreshed immutable receipt is 643 of the 687 benchmark lines; excluding that data artifact, the split is 315 runtime (31.9%), 44 evidence infrastructure (4.5%), and 627 tests/docs/support (63.6%). Receipt volume is validation hygiene, not product progress. The epic remains open.

**2026-07-25T20:49:13Z**

Untouched effectiveness preflight checkpoint through `fbf326b`. A frozen local campaign ran baseline versus sampled `LabeledFewShot(k: 8)` on the same balanced eight-row support-ticket test for seeds 17/23/31, with the test excluded from optimization and selection. Its paired lifts were -0.125, 0.0, and +0.125; mean lift was 0.0, the exact paired-bootstrap 95% interval was [-0.125, 0.08333], one of three seeds improved, and the predeclared within-task go rule did not pass. This is an honest neutral result on one synthetic task and one local model, not a general optimizer win or loss and not C3 evidence.

The identically frozen OpenRouter-free v1 campaign stopped after the first baseline response produced no typed prediction. Its exact-free safety boundary remained intact: requested model `openai/gpt-oss-20b:free`, OpenRouter routed to Darkbloom without fallback, one logical request equaled one transport attempt, usage was 287 input and 64 output tokens, and both provider-reported and computed cost were $0. No arm or seed pair completed, so the run is incomplete rather than an optimizer loss. The immutable v1 artifact remains unchanged at `benchmarks/results/support-ticket-lift-openrouter-free-20260725.json` (SHA-256 `0a56213d72ace84c051eadf59b11914d15fff04485ac5f580d6bc8a287186cc0`).

The response consumed the full 64-token allowance, so truncation is plausible. Whether truncation actually occurred is unknown: v1 did not retain the provider finish reason, bounded raw-output evidence, or the underlying adapter/decoder error. Future strict runs now retain those fields plus route/model, logical/transport attempt counts, tokens, and both cost views through evaluation cancellation and campaign summaries. A no-network malformed typed-response test proves this retention path and immediate one-attempt stop behavior.

`benchmarks/config/support-ticket-lift-openrouter-free-v2.json` preregisters but does not authorize or launch a separate v2. It freezes the same task, dataset digest, train/test boundaries, seeds, arm order, baseline, LabeledFewShot options, model, routing constraints, metric, scoring, and go rule. Its sole protocol change is a bounded 256-token output envelope: four times the ceiling reached by v1, still small for a one-field JSON answer, and intended to separate measurement truncation from task scoring rather than tune toward desired scores. v1 remains stopped/incomplete. V2 cannot establish C3 or BEAM-native superiority, and canonical multi-seed GSM8K/Banking77 plus matched-upstream optimizer effectiveness remains open.

**2026-07-25T20:59:28Z**

The manifest-bound OpenRouter-free v2 was launched once from clean `main` at `8bb6842` only after the public-surface, package/clean-room, integration, reproduction, authority, research-portfolio, upstream-conformance, dashboard, and default checks passed without claim/source drift. Its committed protocol was not changed after launch.

V2 stopped as required on the first malformed typed response: seed 17, baseline arm, evaluation row index 4, after four valid typed responses. The fifth transport response returned an empty content string; the retained adapter error is `%Imp.AdapterParseError{message: "LM response cannot be serialized to a JSON object.", reason: ""}`. Unlike v1, the evidence decides the truncation question for this response: provider `finish_reason` was `length`, output usage was exactly 256 tokens against the requested 256-token ceiling, and both truncation indicators are true. This establishes an output-envelope failure at 256 tokens for that response; it does not retroactively prove v1's truncation, which remains unknown from v1's frozen evidence.

All five calls preserved the authorized boundary: exact requested/actual model `openai/gpt-oss-20b:free`, OpenRouter to Darkbloom, no fallback drift, five logical requests equaled five transport attempts, 1,414 input and 1,106 output tokens cumulatively, and both provider-reported and computed cumulative cost were $0. The campaign stopped with no completed baseline arm, no optimized arm calls, no paired seed scores, no interval, and no optimizer outcome. The predeclared go rule is false by incompleteness, not by measured optimizer loss. The unchanged raw artifact is `benchmarks/results/support-ticket-lift-openrouter-free-v2-20260725.json` (SHA-256 `bc0dac259d31f605f2f94f1b81fe29a38ed5855fb084a596341674f0658c823e`). No C3 or BEAM-native superiority claim is supported; the effectiveness frontier remains open.

**2026-07-25T21:28:00Z**

V1 and V2 are permanently closed as stopped/incomplete campaigns. Their retained evidence cannot determine whether reasoning consumed V1's 64-token envelope, and V2 did not retain reasoning fields. The current adapter path was therefore tested separately from optimizer effectiveness: local request capture proved that an explicit strict JSON schema reaches the provider request and that provider `content` and `reasoning` are mapped separately into typed output and native-reasoning metadata.

The authorized synthetic format campaign then made exactly one guarded call to each preregistered exact-free candidate, with no retry and no support-ticket material. `google/gemma-4-26b-a4b-it:free` returned the strict typed value with `finish_reason=stop` in 12 of 128 output tokens and no native reasoning; exact requested/actual model, OpenRouter-to-Darkbloom route, one logical/transport attempt, and both zero-cost views matched. `nvidia/nemotron-nano-9b-v2:free` made one transport attempt but OpenRouter returned 404 because no endpoint satisfied the required data-deny privacy policy; it is operationally unsuitable under the campaign guard. `openai/gpt-oss-20b:free` returned the strict typed value with `finish_reason=stop` in 29 of 512 output tokens, including 10 reasoning tokens and separately retained native reasoning; exact route, one attempt, and zero costs matched. This proves the current content/reasoning mapping and shows that reasoning can consume the output envelope, but it does not retroactively recover V1/V2's missing reasoning evidence.

The unchanged canary artifact is `benchmarks/results/openrouter-free-typed-format-canary-20260725.json` (SHA-256 `6c0e99e9f6deb558c7dda83b66e516a9563397dc72a361eedd9418e3654d0ddd`). Based only on strict schema correctness and the smaller no-reasoning envelope, Gemma is the sole recommended V3 candidate. `benchmarks/config/support-ticket-lift-openrouter-free-v3.json` preregisters but does not authorize or launch that campaign. It preserves the frozen data digest, untouched indices, seeds, baseline and LabeledFewShot arms, k/sampling, schedule, metric, go rule, 48-call ceiling, one-transport rule, and exact-free safety constraints; it adds the strict task schema, selects exact-free Gemma, and freezes a 128-token ceiling before any V3 task call or score. One successful format call is not route-reliability or optimizer-effectiveness evidence, and V3 remains unlaunched pending separate authorization and fresh catalog/privacy-route validation.

**2026-07-25T21:34:00Z**

The explicitly authorized, manifest-bound V3 support-ticket campaign completed without a guard failure. Immediately before launch, the exact `google/gemma-4-26b-a4b-it:free` catalog and endpoint views still reported zero prompt/completion prices, `response_format` support, `structured_outputs` support on the selected free route, and the previously canary-proven Darkbloom privacy-compatible endpoint remained available. The committed runner independently rejected runtime overrides, locally proved all 48 serialized requests, and revalidated the catalog at launch.

On the untouched balanced eight-row test, baseline versus sampled `LabeledFewShot(k: 8)` scores were 0.50 versus 0.75 for seed 17, 0.50 versus 1.00 for seed 23, and 0.50 versus 0.875 for seed 31. Paired lifts were +0.25, +0.50, and +0.375; mean lift was +0.375 and the exact paired-bootstrap 95% interval was [0.25, 0.458333]. All three seeds improved, so the preregistered within-task go rule passed.

All 48 logical requests equaled 48 transport attempts. Every response retained and passed the exact strict schema request audit, used exact requested/actual model `google/gemma-4-26b-a4b-it:free`, routed OpenRouter to Darkbloom, ended with `finish_reason=stop`, and reported explicit zero provider and computed cost. Cumulative usage was 23,516 input tokens, 552 output tokens, zero reasoning tokens, and $0. The immutable result is `benchmarks/results/support-ticket-lift-openrouter-free-v3-20260725.json` (SHA-256 `504f60b5450f43746fa2f26ada45c2eceddf94585ae61f62be759b3ec4dd23e5`). V1 and V2 remain untouched and permanently incomplete.

This is positive held-out evidence for one cheap synthetic support-ticket task, one exact-free Gemma route, and Imp's LabeledFewShot consumer path. It is not an independently matched upstream result, not the canonical GSM8K/Banking77 portfolio, not general LabeledFewShot or Imp optimizer effectiveness, not C3 evidence, and not a BEAM-native superiority result. The epic's broader effectiveness frontier remains open.

**2026-07-25T21:50:00Z**

GEPA operational repair after the provider-campaign lane closed: the default reflection path had asked for JSON but treated an ordinary textual JSON response as the replacement instruction itself, and the adapted upstream exam only asserted that compilation returned a program. The runtime now renders the pinned standalone GEPA v0.1.4 reflective-example prompt, extracts plain and markdown-fenced instructions with independently matched v0.1.4 behavior, and retains typed-map/JSON-text adapter compatibility without installing their envelope. Explicit `feedback_fn` output remains visible to the reflector as an Imp extension; the generic fallback feedback is no longer injected into the source-shaped LM prompt.

The strengthened consumer test makes the proposed instruction improve a second training/validation example, requires GEPA to select it, and asserts the exact decoded instruction installed in the returned program. This proves provider-free operational proposal/application/selection mechanics for one deterministic program, not live C2 evidence, effectiveness, the paper-family campaign, multimodal reflection, or BEAM superiority. The existing 15-case structural artifact and its claims remain unchanged.

**2026-07-25T22:09:40Z**

Optimize Anything arbitrary-artifact repair. The public `run/3` surface rejected a map as soon as any component was not text, so the advertised artifact examples were textual/JSON-string wrappers rather than native structured values. A second runtime bug dropped `engine.raise_on_exception` at the GEPA bridge, causing proposal failures to abort even when the public configuration requested containment. Both behaviors were reproduced before repair.

Pinned GEPA v0.1.4 remains the text authority: its public API explicitly accepts `str | dict[str, str]`, and an independently executed public run with two named text components produced the baseline, two selected improvements, and distinct train/selection evaluator calls. Imp now preserves that path and adds a clearly labeled native extension for JSON-safe mixed-type maps, with `__imp_type__` reserved at every depth for the existing durable wire codec. The seed derives an exact recursive schema; evaluators and custom proposers receive native immutable artifacts; reflection responses must decode as complete strict JSON values; malformed, partial, type-changing, and no-op proposals are rejected; and candidates are decoded at public trajectory, result, history, rejection, best-output, and persistence boundaries. The shared GEPA engine alone sees a tagged canonical text representation for hashing and durable resume. Refiner, merge, external tracking, custom callbacks, and custom selector/policy extensions fail before execution in structured mode rather than receiving a substitute representation.

Provider-free execution optimized a nested artifact containing strings, booleans, integers, floats, lists, maps, and null from validation score 0.0 to 1.0. The same run proved proposal/evaluator invocation, candidate isolation, validation selection, `best_candidate/1`, result JSON round-trip, atomic checkpoint output, exact resume, and fail-closed checkpoint identity tampering. Adversarial runs covered malformed JSON, missing nested fields, type drift, no-op proposals, and evaluator exceptions. Train and selection examples were observed during optimization; an untouched test example was not evaluated until after selection. This establishes generic native structured-loop mechanics only. It is not upstream schema-v2 compatibility, a rerun of the separate three-domain campaign protocol, held-out effectiveness, C2/C3/C4 evidence, or BEAM superiority; the existing Optimize Anything effectiveness and matched-upstream claims remain open.

**2026-07-25T22:39:07Z**

Optimize Anything batch-evaluation repair. The public runner rejected `batch_evaluator` outright, despite the pinned GEPA v0.1.4 entry point supporting a batch transport with or without a scalar evaluator. A second deterministic failure let a proposed artifact with one successful row and one lost row average to `0.5`, pass acceptance, and become the selected artifact. Both failures were reproduced through ordinary `Imp.Optimize.Anything.run/3` calls before repair.

Imp now sends every pending candidate/example pair to one ordered callback, preserves result partitioning across candidate batches, unwraps string candidates, exposes `nil` for the single-task example, keeps structured artifacts native, supplies aligned optimization states to arity-two callbacks, and ignores the output slot of pinned-compatible legacy triples so a backend cannot replace candidate identity. Scalar and batch transports can coexist. The optimization-state buffer is included in engine checkpoints and restored on resume; a stopped-at-iteration-one run followed by JSON checkpoint resume produced the same exact callback sequence as an uninterrupted run, with no duplicate evaluation calls.

Contained whole-call failures and explicit per-row errors retain aligned score-zero diagnostics, while malformed counts and result shapes remain loud. The shared engine now marks incomplete results, refuses an incomplete seed baseline, rejects incomplete mutation/merge/strategy validation results before admission, and does not cache them. Exact upstream v0.1.4 batch tests passed 24/24 independently; Imp's focused adapter/runner and sequential/parallel GEPA slice passed 57/57. This establishes public batch transport mechanics and stronger failure admission semantics, not optimizer effectiveness, complete public-entry parity, C2/C3/C4, or BEAM superiority. Configurable strategy/application boundaries remain under audit.

**2026-07-25T22:56:30Z**

Optimize Anything custom-batch-strategy repair. The public nested config accepted any atom or struct as `reflection.batch_sampler`, but `to_engine_options/1` dropped the setting and every ordinary run silently used the built-in epoch-shuffled sampler. Exact pinned GEPA v0.1.4 source forwards a custom sampler instance unchanged and rejects combining it with `reflection_minibatch_size`; the ignored Imp setting was therefore a runtime facade, not an unexercised extension.

Imp now exposes a stateful `Imp.Optimizer.GEPA.BatchSampler` behaviour, forwards the configured struct through the public runner, derives the strategy-owned positive batch size, validates ordered in-range indexes and returned RNG/struct identity, and checkpoints module, stable consumer identity, and callback state. A two-iteration ordinary `Anything.run/3` consumer forced distinct training rows; stop-after-one plus JSON resume produced the exact uninterrupted evaluator sequence and selected result, while identity tampering failed before another evaluator call. Built-in schema-7 checkpoints remain byte-shape compatible; schema 4-6 checkpoints explicitly refuse a custom strategy because those formats contain no custom state. This establishes real configurable sampling and durable native resume mechanics, not Python object-mechanics parity, optimizer effectiveness, C2/C3/C4, or BEAM superiority. Candidate application and the other configurable strategy boundaries remain under audit.

**2026-07-25T23:08:04Z**

Optimize Anything public-strategy audit hard stop. Pinned v0.1.4 exposes proposal sampling, proposal selection, and acceptance criterion through `EngineConfig`, but Imp's nested public config omitted all three even though the shared GEPA engine already implemented their native mechanics. It also mapped `max_candidate_proposals` to both iteration count and reflection-call budget; that stopped a multi-proposal round after its first reflection even though upstream counts proposal rounds. The missing configuration was reproduced through the ordinary consumer boundary before repair.

Imp now exposes validated built-in sampling (`:single`, same-parent, independent, and P-by-N), proposal selection (all, best, top-K, or BEAM callback), and acceptance (strict, improvement-or-equal, or BEAM callback) settings. The public bridge derives the complete task width, forwards each policy, keeps proposal-round and reflection-call budgets distinct, and includes semantic policy identities in engine checkpoints. A three-proposal public run invoked acceptance exactly once per proposal, passed all proposals together to selection, retained only the largest improvement, returned the selected string, and executed it from a fresh consumer process. Equal-score admission differs under the two configured criteria. JSON resume succeeds with unchanged policy and rejects same-width sampling, selection, or acceptance substitution before another evaluator call. Built-in tuple strategies round-trip through config JSON; unsupported custom Python sampling/criterion objects are rejected explicitly. Exact pinned sampling/selection/stateful-acceptance tests passed 9/9 independently.

Pinned Optimize Anything itself returns an immutable result and `best_candidate`; it does not apply by mutating an arbitrary consumer object. Imp preserves that intended boundary: `best_candidate/1` returns the native artifact for explicit installation, and unknown application options are already rejected by the runner. This closes the bounded public-strategy/application audit, but establishes neither whole Optimize Anything parity nor effectiveness, C2/C3/C4, or BEAM superiority. Per principal direction, further OA expansion stops here and the next large capability lane is actual weight-changing mmGRPO/GRPO plus BetterTogether continuation into a reusable artifact consumed by a fresh program/process; Fast-Slow/CISPO remains external and separate.

**2026-07-25T23:52:26Z**

Supported MLX SFT deployment implementation checkpoint. `MLXLMTrainer` now treats official fusion as part of successful training: schema-2 manifests bind the pinned base-tree digest, adapter files, fusion and server commands, and a complete fused-tree inventory. The job exposes the canonical fused directory, not the adapter or a renamed base. Failed fusion resumes only from the content-verified adapter without repeating SFT; mutated adapters, manifests, completed trees, partial trees, and base-identical inventories fail closed.

An application-supervised `MLXLMDeployment` revalidates the fused contents, starts the recorded loopback `mlx_lm.server` process group, and accepts readiness only when `/v1/models` advertises exactly the canonical fused path. `TrainingJob.rebind/3` can now install that ReqLLM automatically, records the fused digest in program metadata, and supports checksummed job/program save-load followed by exact-artifact rebind in a fresh BEAM process. Explicit stop verifies external process-group cleanup. Ordinary `BetterTogether` execution with strategy `[:w, :p]` proves that the prompt optimizer and final program call the rebound trained endpoint after `BootstrapFinetune`; it does not merely observe a new model label.

Focused deterministic execution passed 139 tests with zero failures (one unrelated exclusion), including fusion-only retry, adapter/tree/manifest tamper refusal, wrong server identity and cleanup, a real local HTTP ReqLLM call, job/program persistence, fresh-process rebound, and BetterTogether weight-to-prompt continuation. The executable fixture returns planted base/trained behavior, so this proves lifecycle and consumer mechanics only. The required real MLX frozen probe—same task input against the pinned base and fused model, followed by reproduction of the fused output from a fresh consumer—remains unexecuted pending the coordinated heavy slot. It will be trained-behavior/lifecycle evidence, not general effectiveness. The prior admitted Banking77 MLX artifact remains separate effectiveness evidence and has not been reinterpreted.

GRPO remains open and separate. Inspection recommends pinned TRL `v1.6.0` (`0dac440542c2ef9b575f56534f29f6fca1febe4a`) as the first mature external training-engine target. Community `mlx-lm-lora` `v3.0.0` (`fb4f39db66fadec3b71a41441e863d9f1bf87844`) really changes MLX weights but remains experimental pending dependency/license, loss/resume, artifact, and duplicate reward-callback audits. The proposed external scoring boundary is immutable, ordered, content-hashed, idempotent batch data with persisted receipts before gradient acceptance; no GRPO engine or claim was added in this slice.

**2026-07-26T00:14:51Z**

The single approved real-MLX Banking77/Qwen2.5 lifecycle attempt stopped before starting MLX because its frozen input contract was internally stale. The exact approved model revision downloaded successfully, and the nine runtime files matched the preregistered 289,598,797-byte inventory and tree SHA-256 `047d24a10e4acc788e046734351a0e4ec668ee36d2d9453daeb80a9364e87947`. The frozen 80-row source pool and 72/8 counts also matched. However, the preregistered rendered train/selection JSONL digests (`b4e9c021…` and `c108e3c7…`) came from the admitted legacy artifact whose manifest names `Elixir.DSEx.Adapter.Chat`; clean current Imp at `4ffe3de` rendered `849c8bee…` and `ff3223c6…`, combined digest `e8a3dfc5…`, through public `Imp.Adapter.Chat`.

The fail-closed guard stopped the attempt after 46 ms. No `mlx_lm` package execution, training job, fusion, inference, server, provider call, or spend occurred; both reserved ports were free and no MLX process remained. The immutable stopped result is `benchmarks/results/local-mlx-lifecycle-banking77-20260725.json` (file SHA-256 `7010a51c317a66e00132347577e45ae01284537deedf6a9f46f2346101e2226c`, payload SHA-256 `868cede844819ead2b1f9272835575bf4e695a2c9c5a12a5fbde5757499521ad`). It is an input-identity failure, not an SFT result, optimizer loss, deployment failure, or effectiveness observation. It must not be resumed or reinterpreted. The real fused lifecycle acceptance remains open and requires a separately reviewed contract whose split receipts are frozen from current Imp before launch; this attempt receives no retry.

**2026-07-26T00:22:50Z**

The rendering drift is now reproduced exactly from the historical source, not inferred from manifest names. Loading `DSEx.Adapter.Chat` from `ada199ba354adfd24b77be5ff9fa8eb3d7be7f3d` through the unchanged deterministic dataset builder reproduces the legacy `b4e9c021…` / `c108e3c7…` / `7d12c549…` receipts byte-for-byte. Both renderers use `system`, `user`, `assistant` rows and keep the routing objective in the system message and utterance in the user message. Current Imp additionally renders the enum as `Literal['R17', 'R42', 'R68', 'R93']`, includes the exact-match/type requirements in the system and user messages, and terminates the assistant target with `[[ ## completed ## ]]`. Those are intended current semantics: `docs/ADVANCED.md` selects `Imp.Adapter.Chat`, `test/provider_training_lifecycle_test.exs` pins the exact fine-tuning row including the completed marker, and `test/upstream_exam/adapters_test.exs` pins the DSPy-shaped system constraints.

Because split ranking hashes complete rendered row bytes, the content correction also changes derived membership and order without changing any of the 80 source rows: seven of eight selection identities move, the train sets share 65 rows, only 2/72 train positions and 1/8 selection positions retain the same identity. Draft `benchmarks/config/local-mlx-lifecycle-banking77-v2.json` freezes the current `849c8bee…` / `ff3223c6…` / `e8a3dfc5…` receipts and exact row order while preserving V1's dataset, model, hyperparameters, test set, metrics, and interpretation. It is separately named, explicitly linked to permanently stopped V1, unlaunched, and unauthorized pending principal review. No training, inference, server, provider call, or new data selection occurred during this audit.

**2026-07-26T00:30:41Z**

Principal-authorized V2 executed exactly once from clean `c51a99ecdb2d631019c6c3e3569924a44f9d3ec8` and then stopped at the first owning deployment failure. The committed contract and all current-rendered 72/8 row identities and digests matched. Pinned MLX-LM 0.31.3 completed all 216 LoRA steps in 102,288 ms (validation loss 3.668 at step 1 and 0.069 at step 216; final train loss 0.104; peak reported memory 1.182 GB), produced the hashed adapter, and completed official fusion in 1,306 ms. Local `MLXLMTrainer.verify_job/1` accepts the retained job and fused tree; the fused tree SHA-256 is `d52aca6b049e9e015b0934bc56e4a120a6d669fb21bc5c7068c527e0f994dc44`, distinct from the pinned base tree `047d24a…`.

The base server/evaluation stage completed in process, but its rows were not persisted before the later failure and therefore are not result evidence. `TrainingJob.rebind/3` then refused the fused deployment because MLX-LM's `/models` response advertised both the expected exact fused path and unexpected `jxm/gpt-oss-20b-base`. The guard cleaned up and both reserved ports are free. No fused prediction, base-versus-fused comparison, saved program, or fresh-process reproduction occurred; this is a stopped lifecycle measurement, not positive, neutral, or negative effectiveness evidence. Immutable result `benchmarks/results/local-mlx-lifecycle-banking77-v2.json` has file SHA-256 `4feb63c01e1ac7ba8322dfd86051c3bae1f5ad20adc5b3900d74cfda38f24005` and payload SHA-256 `a15f632f3d8668d09be8589442f0f88927e160f06ba75c9f0ab693f2bc9d63c9`. V2 receives no retry; the verified trained artifacts remain retained for read-only diagnosis and any separately reviewed deployment-only continuation.

**2026-07-26T00:39:46Z**

Pinned MLX-LM 0.31.3 source resolves the deployment failure without weakening Imp's identity contract. `/v1/models` is a Hugging Face cache-discovery endpoint, not loaded-model inventory: it advertises every cached `main` model with the three expected config/index/tokenizer files, then appends the resolved CLI `--model` path. `jxm/gpt-oss-20b-base` is not a static server literal or alias. It is a genuine 39 GB model at revision `4bdf551f…` in the machine's default Hugging Face cache, whose `main` ref predates V2 by almost a year. It was therefore ambient contamination of discovery, although MLX-LM could hot-load it if a request explicitly named it.

The exact fused path remains source-provable as V2's sole startup-loaded and routed artifact: the launch argv passed it as `--model`; `ModelProvider` loads that default before generation; Imp's deployment LM sets both ReqLLM model fields to the exact path; ReqLLM encodes that ID in the JSON request; MLX-LM loads the requested model key and echoes it in the response; and Imp retains the echo in response metadata. No complete launch-environment snapshot or server stdout/stderr survived cleanup, so those details remain unknown rather than reconstructed.

Draft `benchmarks/config/local-mlx-lifecycle-banking77-v2-deployment-continuation.json` proposes one deployment-only continuation from the content-verified retained fused tree. It does not resume V2, train, fuse, weaken the exact-single-ID guard, or touch the ambient cache. Instead it gives parent and fresh BEAM processes an empty isolated Hugging Face cache, keeps offline mode and exact local paths, repeats the frozen base/fused selection and untouched-test evaluation because the base rows were not retained, requires exact request/response model identities for every row, saves and reloads the job/program, reproduces fused predictions byte-for-byte, and cleans up every process. The draft is unlaunched and unauthorized pending principal review and a heavy slot.

**2026-07-26T00:54:59Z**

The principal-authorized deployment-only continuation executed exactly once from clean `47c7caa1fea064030ebd0131b667f415b7235e14` and stopped at its first owning measurement failure after 2,654 ms. No-server preflight had matched the reviewed contract SHA `54f85d73…`, pinned source, retained job/manifest, base and fused inventories, frozen 8/40 row identities, empty isolated cache, and free ports. The launched script SHA-256 was `0c45dac47203f9ac98fb5668a916cd4e09a629802b7c0345ac32a7e0d26fc6e4`.

The exact base server advanced past the strict single-ID `/models` check to the first frozen base evaluation call. That call returned to the runner with two identical ReqLLM request-start lifecycle events for the exact base path. Because HTTP retry was explicitly disabled (`retry: false`, `max_retries: 0`) and the frozen runner treated more than one request-start event per row as a no-retry guard violation, it stopped immediately. This artifact does not establish whether the duplicate lifecycle events represented multiple transport attempts; no transport-attempt counter or completed row was retained, so that meaning remains unknown rather than inferred.

No base evaluation stage was admitted, and no fused deployment, fused evaluation, saved job/program, or fresh-BEAM reload occurred. The retained trained/fused tree was not modified; no training, fusion, alternate model, provider call, download, retry, or substitution occurred. The isolated Hugging Face cache contained no model directories. Cleanup succeeded, ports 18821 and 18822 are free, and no MLX server remains. Immutable stopped result `benchmarks/results/local-mlx-lifecycle-banking77-v2-deployment-continuation.json` has file SHA-256 `b22389493e1f71373c9e05d7447b33ebeff7cce899413ef6d7920753c4bb565d` and payload SHA-256 `0ee59a5b5a7bd050fa88237c67891b9faa6c2f5ffd2f496b6dcf98583fb8547a`. The continuation receives no retry and yields no score, trained-behavior, lifecycle-pass, or effectiveness evidence.

**2026-07-26T01:24:21Z**

The completed plain public-consumer acceptance is preserved unchanged as `benchmarks/results/local-mlx-public-consumer-banking77-dd6f6ad.json` (SHA-256 `999a5d5aa1f4921927f83ef14d233b25f5914c4988c748ce5d3a96093dfe2b35`). On the frozen four-intent Banking77 subset, one pinned Qwen2.5-0.5B MLX SFT artifact improved untouched 40-row accuracy from `0.125` to `0.55` and macro-F1 from `0.0610` to `0.4561`. The trained program was saved, loaded, and served from the exact fused artifact in a fresh OS BEAM, reproducing ordered fused predictions and errors byte-for-byte.

This result is scoped to that artifact, model, task subset, split, and run. It does not establish general Imp or SFT effectiveness, GRPO, production reliability, DSPy parity, or BEAM superiority. The next substantive implementation family is the actual GEPA operational/paper-family program workflow; evidence receipt work does not continue as its own lane.

**2026-07-26T01:50:00Z**

The single frozen provider-free GEPA Banking77 consumer run is incomplete. Its first launch stopped before any model call because the runner supplied a metric outside GEPA's documented arity-two public contract; commit `a0cc920` corrected that harness error and added a no-model constructor/execution regression without changing the model, data, splits, prompts, optimizer, or budgets. The authorized continuation completed optimization and the base-test stage, then stopped after selected-test calls because its instruction-use recorder searched for the raw multiline instruction inside `Imp.Adapter.Chat`'s canonically indented objective rendering. The selected-test rows and aggregates were not written before that guard and are therefore unavailable rather than reconstructed.

The selected named-predictor instruction is byte-identical between the selection record and saved parameter artifact. Loading and applying that artifact to a fresh compatible ordinary program preserves its runtime bindings, and no-model rendering proves the selected instruction reaches the adapter's canonical objective representation. This establishes narrow parameter persistence/application behavior, not selected transport use or an optimizer outcome. The completed base stage made 80 logical calls and 80 transports for 40 two-predictor rows; 38 classifier outputs failed the strict typed parser because the local `llama3.2:3b` model emitted `[[ Rxx ]]` instead of the explicitly requested `[[ ## route ## ]]` field marker, while the two parsed outputs were incorrect. This is poor format adherence for this local 3B model and frozen task, not general GEPA effectiveness evidence.

The ordinary recorder now checks source instructions against the adapter's canonical rendered objective and atomically persists a completed stage before running later acceptance checks. Focused no-model regressions cover multiline rendering identity and write-before-validation durability. The stopped run will not be rerun, normalized, or promoted; selected transport behavior, untouched selected-program behavior, fresh-process prediction reproduction, and GEPA held-out effectiveness remain open.

**2026-07-26T02:19:51Z**

The first true-GRPO implementation slice establishes the narrow Imp↔TRL transaction boundary without pretending that a trainer exists. `Imp.Clients.TRLProtocol` pins TRL v1.6.0 and seals recursively allowlisted session, update, receipt, checkpoint, and artifact envelopes with canonical SHA-256 identities. Sessions bind ordered dataset rows and the deterministic prompt schedule. Updates bind rendered prompts, grouped completion text, prompt/completion token IDs and masks, behavior log probabilities, externally computed finite rewards, group/order identity, trainer step, optimizer state, RNG state, and an exact idempotency key. Missing, reordered, tampered, and same-step/different-payload values fail closed; exact replay returns the already accepted state.

The ordinary `Imp.train/4` GRPO path now returns a completed `TrainingJob` alongside its rebound program. A deterministic long-lived no-model server accepted two ordered updates, wrote the sealed intent before synthetic artifact mutation, published versioned checkpoint/receipt/artifact trees, and rebound the content-verified result into a saved program from a fresh OS BEAM. An accepted-then-disconnected step reconciled its durable receipt without a second update. `TrainingJob.rebind/3` independently verifies the TRL manifest, every file, checkpoint, ordered update and receipt chain, base/session identity, and exact deployment path; same-size artifact tampering was refused.

This proves protocol, orchestration, crash-window, artifact-verification, and fresh-program lifecycle mechanics only. The conformance artifact is synthetic. No TRL/PyTorch installation, Python worker, causal model, tokenizer execution, GRPO loss, gradient, optimizer update, model-weight change, Apple/MPS feasibility, held-out improvement, mmGRPO parity, general effectiveness, or BEAM superiority is claimed. The next substantive slice remains a separately reviewed pinned TRL worker feasibility gate and then one real LoRA GRPO step under this existing boundary.

The separately bounded real-TRL slice added a supported pinned local worker in `1beb3f2`: `Imp.Clients.TRLTrainer` supervises one TRL v1.6.0 process, and `Imp.Clients.TRLLM` lets ordinary Imp rollouts use that same causal policy. The worker accepts mutation only through the existing sealed protocol, refuses credential-bearing environments, network/cache fallback, CPU fallback, alternate operations, uniform rewards, model/dependency drift, and replay ambiguity, and is designed to retain TRL advantages plus pre/post trainable-tensor, optimizer, RNG, checkpoint, and artifact identities. The exact CPython 3.12 / TRL 1.6.0 / Transformers 4.57.6 / PEFT 0.18.1 / PyTorch 2.10.0 lock and seven-file Qwen2.5-0.5B-Instruct revision were verified locally; MPS was available.

The single authorized feasibility execution then stopped before protocol acceptance or training. Four real causal-model rollouts completed, but `TRLTrainer` encoded the atom-keyed Imp batch with the lossless tagged-term codec while the worker expected the plain JSON projection. The worker therefore saw no plain `group` sample list and rejected the preparation with `generation_count_mismatch`, `accepted=false`. No sealed update, TRL trainer construction, relative advantage, optimizer step, checkpoint artifact, or tensor mutation occurred. The worker exited cleanly. `benchmarks/results/local-trl-grpo-feasibility-20260725.json` preserves that stopped outcome. The transport projection is repaired and covered without another model call, but the explicitly one-shot slice was not rerun; real MPS GRPO weight-change feasibility remains unknown and open.

Principal-authorized continuation from clean `5b04e3c` reused the identical verified model/environment, prompt, seed, four-sample group, reward, MPS-only settings, and one-update acceptance. The repaired plain projection reached the intended semantic guard, which rejected the group with `uniform_rewards`, `accepted=false`. Thus four real causal-model rollouts completed again, but the update was not sealed and TRL still did not construct a trainer, compute relative advantages, take an optimizer step, or change tensors. The worker exited cleanly and the continuation was not retried. The prior recorder ordering overwrote the completed step intent during termination failure, so the retained record cannot determine whether all four rewards were zero or all four were one; it states that uncertainty rather than reconstructing outcomes. `TRLTrainer` now atomically persists the plain projected completions/rewards before worker validation so future pre-accept failures cannot erase that stage. Real MPS GRPO weight-change feasibility remains unknown; uniform sampling is a valid frozen-gate stop, not optimizer loss or ineffectiveness evidence.

**2026-07-26T03:03:41Z**

The separately authorized controlled external-rollout engine-conformance step executed once and stopped before protocol acceptance. The ordinary Imp GRPO path supplied ordered semantic values `R17/R42/R68/R93`, and `Imp.Adapter.Chat` correctly rendered them into its canonical assistant field representation before trajectory capture. The worker's newly added controlled-order guard incorrectly compared those rendered completions byte-for-byte with the source values and returned `controlled_rollout_order_mismatch`, `accepted=false`. The raw projected group was durably retained and proves ordered rewards `[1,0,0,0]`; because preparation stopped during that mistaken comparison, no sealed update, `GRPOTrainer`, relative advantage, optimizer step, tensor mutation, artifact, or rebind occurred. The worker exited cleanly. This is a stopped harness/runtime integration result, not GRPO feasibility, sampling, learning, or effectiveness evidence, and it will not be rerun under the one-execution authorization.

The ordinary controlled-rollout seam now distinguishes the explicit external source from model generation, accepts only the stable source-to-canonical-adapter rendering identity, and persists the fully tokenized/log-prob-scored group before subsequent semantic validation. A no-model regression pins the source/render mapping. This post-stop repair is unexecuted against TRL/MPS; real weight-update feasibility and fresh artifact rebind therefore remain unknown.

**2026-07-26T03:07:55Z**

The one authorized continuation from clean `b70cebf` stopped even earlier at GRPO checkpoint reconciliation with `GRPO session checkpoint identity mismatch`. No worker update preparation, sealed protocol update, trainer construction, policy scoring, advantage, optimizer step, tensor mutation, artifact, or rebind occurred in the continuation; cleanup remained complete and no retry followed.

The end-to-end boundary reduction isolates the mismatch to callback identity rather than model, prompt, completion, reward, device, LoRA, or protocol configuration. The checkpoint persisted the runner-local anonymous reward function using Erlang `fun_info` module, name, type, `uniq`, and index. The continuation required recorder-only edits to preserve the predecessor result; those edits recompiled the surrounding script module and changed its compiler-local callback identity even though the callback body and treatment were unchanged. This does not establish that a byte-identical runner would fail across a fresh OS restart, but it does show that treatment-neutral harness changes cannot resume this session. Rewriting the saved identity would also rewrite the session/optimizer identity and is therefore not a safe continuation. The predecessor's exact raw projected group remains retained; the repaired source-to-canonical-render check and the full tokenized/log-prob group were not reached.

Static source reduction from the Imp program through pinned TRL 1.6.0 confirms the remaining intended boundary: the adapter canonicalizes each supplied source route; the worker tokenizes prompt and canonical completion and computes exact Qwen behavior log probabilities; the unchanged sealed update carries ordered token IDs, masks, log probabilities, and rewards; `rollout_func` returns TRL's required `prompt_ids`, `completion_ids`, and `logprobs`, while `external_rewards` is forwarded as an extra input field to the reward function. None of the TRL-side path was executed here. A future resumable public GRPO run that permits treatment-neutral harness changes requires an explicit stable reward/validation callback identity rather than compiler-local anonymous-fun identity; this controlled run is closed and establishes no MPS weight update, sampling, learning, effectiveness, parity, or BEAM superiority.

**2026-07-26T03:24:00Z**

Durable GRPO callbacks now have an explicit public data contract. `Imp.Optimizer.GRPO.Callback.reward/3` and `validation/3` bind an already-loaded module/function to a consumer-owned versioned id and JSON-safe configuration SHA-256. Reward functions receive the config as their third argument; validation functions receive it as their fourth. Durable identity stores module/function names, kind, id, and config digest only. It neither serializes executable code nor creates atoms while loading saved state. A mutated struct whose config no longer matches its digest fails validation.

When `checkpoint_path` is set, GRPO now refuses bare reward or validation functions before trainer capability checks, worker/session startup, model execution, or checkpoint creation. Non-durable in-process GRPO retains the existing arity-one/two/three function surface. The stable callback identities are included in the checkpoint identity and therefore in the trainer session optimizer-config digest. The controlled MPS runner now uses the public stable callback form but was not launched again.

A deterministic file-backed trainer proved the lifecycle across separate OS BEAM processes: process one durably accepted session startup and was killed by the bounded callback timeout; process two reconstructed the same public program and stable callback, reconciled once, observed the unchanged reward `1.0`, completed one step, terminated, produced the artifact, and removed the checkpoint. Changing either the callback id or config in process two was rejected by checkpoint identity before reconciliation, step, model call, or artifact activity. Anonymous durable use was likewise rejected before the trainer recorded any event.

The reduced callback boundary is no longer process-local. Two adjacent compatibility surfaces can still carry process-local `fun_info` identities if consumers place functions inside trainer structs or `train_kwargs`; those are not reward/validation callbacks and were not broadened in this repair. Absolute trainer/model/root paths also remain host-local by design. Dataset, prompt schedule, model string, seed, optimizer settings, stable callback identity/config, and persisted RNG/session ids are data identities rather than live-process identities. Real TRL/MPS advantage, optimizer, tensor-change, artifact, and rebind execution remains unproven and no effectiveness claim changes.

**2026-07-26T03:45:00Z**

The exact no-model fresh-OS preflight from clean `bc8c92f` completed create, durable checkpoint, fresh-process reconciliation, the ordered four-completion/reward group, sealed protocol update, artifact, and fresh-program rebind. Its inventory is deliberately limited to identities actually persisted on this controlled path: model, dataset rows, prompt schedule, seed/RNG, optimizer settings, stable reward callback module/function/id/config digest, trainer module/root, session/step/idempotency digests, and artifact identity. `train_kwargs` was empty and the trainer path carried no trainer or train-kwargs function identity, so no broader function-identity claim is made. The preflight made zero model calls and used deterministic byte tokens with `0.0` log probabilities.

The one authorized real MPS continuation then reached genuine Qwen tokenization and behavior-policy scoring for all four controlled completions, durably retaining their exact ordered token IDs, masks, log probabilities, and rewards `[1.0, 0.0, 0.0, 0.0]`. It stopped before protocol acceptance with `envelope_digest_mismatch`; no TRL trainer, group-relative advantages, optimizer step, tensor mutation, artifact, or rebind occurred. Cleanup completed and this lane is stopped rather than patched or retried.

Read-only reduction identifies the architectural seam. `Imp.Clients.TRLProtocol` claims cross-runtime canonical SHA-256 identity while Elixir/Jason and Python `json.dumps` independently encode floating-point values. Real log probabilities expose valid but byte-different JSON spellings: for example Python emits `-0.00019822540343739092` where Elixir emits `-1.9822540343739092e-4`, and Python exponent forms use zero-padded exponents where Elixir does not. Consequently the identical retained group hashes to `sha256:71ed006f...` in Elixir and `sha256:9e5622f6...` in Python. The zero-logprob preflight could not expose this. The boundary needs one specified numeric canonical form or a single canonical-byte owner with cross-runtime verification; adding another case-specific continuation would not repair the protocol architecture. This stopped result establishes neither MPS/TRL weight-update feasibility nor learning, effectiveness, parity, or BEAM superiority.

**2026-07-26T08:45:15Z**

The cross-runtime GRPO mutation boundary is repaired and one controlled MPS update completed. `Imp.Clients.TRLProtocol` now names pinned CPython `json.dumps` finite-float spelling as the numeric authority and uses the existing byte-faithful `Imp.PyFloat` renderer instead of Jason's Erlang spelling. Focused worker conformance covers IEEE edge cases, retained Qwen behavior log probabilities, and 2,048 deterministic finite doubles; an additional disposable 9,994-value comparison matched CPython exactly. Protocol schema v1 and the frozen training treatment were unchanged.

The first post-commit execution stopped before acceptance because the command used `--no-compile` with a stale dev BEAM; that stopped artifact is retained and was reduced against the full prepared update. After compiling exact commit `4216ff7`, the unchanged controlled Qwen2.5-0.5B/TRL 1.6.0/LoRA step completed on MPS. Four externally supplied ordered completions carried exact-policy behavior log probabilities and semantic rewards `[1.0, 0.0, 0.0, 0.0]`. TRL computed group-relative advantages `[1.4997000694, -0.4999000132, -0.4999000132, -0.4999000132]`, advanced global step `0 -> 1`, and changed the trainable tensor digest from `cbe6a17e…` to `5cd65e93…`. The content-inventoried artifact identity is `d5363086…`; a saved job/program loaded in a fresh OS BEAM and rebound that exact artifact identity. Worker cleanup completed.

Immutable completed result `benchmarks/results/local-trl-grpo-controlled-update-canonical-current-beam-20260726.json` has file SHA-256 `27efea038e8c3c3e2daca7e73777f04785ab325e33e352a3c19482b0d72b799e`. This establishes controlled external-rollout MPS/TRL weight-update feasibility and fresh-process artifact consumption only. It does not establish model-generated sampling, learning, usefulness, full GRPO/mmGRPO parity, production reliability, effectiveness, or BEAM superiority. Those remain separate runtime and held-out-product gaps.

**2026-07-26T08:53:20Z**

GEPA now exposes the complete safe handoff for consumer-defined multi-predictor programs through `compile_with_artifact/5`. One ordinary call returns the selected live program, independent optimizer report, and checksummed parameter-only artifact. The artifact binds named predictor signatures, demonstrations, configs, selected score, report, and provenance while excluding the consumer module, LMs, adapters, callbacks, credentials, and arbitrary runtime state. Unsupported options and empty artifact identities fail before evaluation.

A deterministic two-predictor consumer began with a failing classifier, used GEPA reflection to select both analyzer and classifier instructions, wrote the returned artifact, reconstructed trusted program code with a fresh LM in a separate OS BEAM, applied the selected named parameters, retained the fresh runtime binding, and executed the selected behavior. The complete focused GEPA slice passed 257 tests with zero failures (one exclusion). This closes a real reusable-program lifecycle gap; it does not prove the stopped llama3.2 selected transports, natural-task lift, paper-family reproduction, general GEPA effectiveness, or BEAM superiority.

**2026-07-26T08:59:05Z**

The shared optimizer-report boundary no longer drops results for consumer-defined multi-predictor structs without a top-level metadata field. `Imp.Optimizer.Report.attach/2` now installs one report through every named predictor when direct metadata is unavailable, and `fetch/1` reconstructs only a consistent report; conflicting or malformed embedded reports fail loudly. This repairs MIPROv2 and SIMBA's ordinary return path and removes InferRules' need to be the sole family with a private custom-program workaround. A real zero-step SIMBA final selection over a two-predictor consumer returned its final score/report through the public compiled program. Focused shared, MIPROv2, SIMBA, GEPA, and report tests passed 62/62. This is runtime observability and resume-state retention, not optimizer effectiveness.

**2026-07-26T09:01:10Z**

Reusable parameter artifacts are now a shared optimizer capability rather than a GEPA-only convenience. `Imp.Optimizer.Artifact.from_optimized_program/2` captures any report-bearing optimizer's selected named predictor state, score, report, and provenance into the existing checksummed parameter-only envelope. It refuses programs without reports, unknown options, empty identities, and reports without optimizer identity. A SIMBA-selected custom two-predictor program produced a shared artifact, applied it to a fresh compatible program, retained the host runtime LM, and executed the selected route. GEPA delegates its convenience method to the same boundary. This enables the same safe post-selection handoff for MIPROv2, SIMBA, InferRules, bootstrap/search families, and GEPA; it does not establish family effectiveness or serialize arbitrary executable state.

MIPROv2 resume no longer admits categorical-search observations beneath a changed task runtime. The compatibility digest now binds each predictor's LM, adapter, demos, config, and dynamic binding policy after `task_lm` rebinding, while continuing to exclude closure captures, process handles, references, ports, credentials, transient traces, and attached reports. Focused JSON resume tests prove an equivalent callback with a fresh captured process remains valid, but task-LM and predictor-config drift fail before another trial. This protects study semantics; it does not add search effectiveness evidence.

The bundled local TRL worker is no longer a Banking77-only conformance fixture masquerading as a general trainer. Its default one-update contract accepts arbitrary Imp-rendered prompts and finite external rewards; uniform rewards may truthfully yield a no-op instead of being rejected by an experiment-specific tensor-change assertion. The older semantic/non-uniform/tensor-change checks remain active only for the explicit retained feasibility contracts. GRPO now supplies a session identity for non-durable in-process jobs, and `TRLTrainer` rejects rollout-count or step-budget mismatch before starting the Python worker or loading the model. No-model tests exercise arbitrary prompt/reward admission, uniform reward groups, pre-model refusal, and the non-durable public session path. Multi-step optimizer/adapter restoration, ordinary model-sampled learning, and GRPO usefulness remain open.

The first ordinary cached-Qwen execution through that general contract completed four genuinely model-generated rollouts, one official TRL optimizer step, a content-verified artifact, and worker cleanup. All four external rewards were zero, advantages were `[0,0,0,0]`, loss was `0.0`, and the trainable digest was correctly unchanged. The run exposed that `TRLLM` was wrapping full model-generated ChatAdapter text as an already-parsed output field; controlled bare labels had hidden that boundary error. `TRLLM` now returns model generations as raw text for the program's real adapter to parse, while explicitly controlled external values retain their typed field wrapper. The zero-gradient run establishes general engine/lifecycle execution only; its scores are invalid for usefulness and it is not a GRPO loss or win.

SIMBA's durable checkpoint now binds the same task-runtime semantics as MIPROv2: predictor adapter, config, and dynamic LM/adapter policy join its existing signature, demos, and LM identity. A deterministic paused run with altered call config/dynamic adapter policy is rejected before another task, prompt, candidate, or final-evaluation call. Live closure captures remain replaceable across processes. This prevents old population scores from being resumed under new parsing or call-safety behavior; it does not add SIMBA effectiveness evidence.

The local TRL result is now an executable reusable program artifact rather than an identity-only rebind. `TrainingJob.rebind/3` requires a caller-reconstructed trusted `TRLTrainer` runtime for completed TRL jobs; saved jobs never restore executable paths. `TRLDeployment` revalidates the complete artifact, loads its PEFT adapter into the exact pinned base model, compares the loaded LoRA tensor digest with the retained post-update digest, and binds every generation to the exact artifact identity. Missing runtime configuration, base/artifact mismatch, tampering, or response identity drift fails closed, and the worker remains explicitly stoppable.

A fresh OS BEAM loaded the retained changed-weight controlled Qwen2.5-0.5B job and portable program, reconstructed the pinned CPython/TRL runtime, loaded the verified adapter digest, executed an ordinary typed Imp call through the trained artifact, and cleaned the worker. Deterministic fresh-process coverage also proves executable rebind and tamper refusal with a no-model worker. This closes trained-artifact consumption for the supported one-update local TRL boundary. It does not change the controlled run's scope or establish ordinary model-generated GRPO learning, held-out usefulness, multi-step resume, full GRPO/mmGRPO parity, production reliability, or BEAM superiority.

SIMBA no longer silently flattens a heterogeneous multi-predictor program onto its first predictor's LM while sampling trajectories. Ordinary rollouts now preserve every predictor's own LM and apply only the shared rollout id/temperature policy; the explicit teacher rollout still substitutes the configured teacher LM across the program. A one-step two-stage regression proves both distinct task LMs execute their own predictor for both sampled rollouts and remain distinct in the returned program. This repairs the search input itself: previously buckets, reflection, and candidate construction could optimize trajectories from a different runtime graph than the consumer supplied.

**2026-07-26T10:20:00Z**

The ordinary local GEPA program front door now has a real-model example rather than only a deterministic artifact fixture. A two-predictor Banking77 router used pinned local `llama3.2:3b` for evidence analysis and the exact retained MLX-fused Qwen2.5-0.5B artifact for strict typed route classification. GEPA saw only the frozen 16 train and 8 selection rows, executed one all-component proposal as two reflection calls, evaluated the mutated program, and selected through validation before any of the 40 untouched rows were called. Both named instructions were observed in their canonical rendered adapter messages, analyzer evidence was handed to the classifier, and every logical call matched one transport attempt with cache and retries disabled.

The result is honestly neutral/negative for optimization. The generated candidate scored `0.0` against baseline selection `0.375`, so GEPA retained the baseline parameter snapshot. On the untouched 40 rows, both baseline and selected programs scored accuracy `0.475`, macro-F1 `0.3333333333`, and zero parse errors. The saved completed training job plus selected parameter artifact reconstructed the trusted heterogeneous program in a fresh OS BEAM, served the identical fused artifact, and reproduced the ordered selected predictions/errors byte-for-byte. The first bounded attempt, preserved separately outside the repository, had accidentally capped two-component reflection at one call and therefore performed only baseline lifecycle work; the example now persists and validates completed reflection before test evaluation.

This closes real local proposal/evaluation/selection, selected-program transport, parameter persistence, and fresh-process consumption for one ordinary task. It does not establish positive GEPA lift, general effectiveness, paper-family parity, provider behavior, or BEAM superiority. The next GEPA research obligation is a separately designed natural-task treatment with enough proposal budget/model quality to test lift without tuning against this negative result.

**2026-07-26T12:35:00Z**

The ordinary local GRPO path has now crossed the semantic-signal boundary without substituting controlled rollouts. In one frozen two-step Qwen2.5-0.5B/Banking77 JSON-adapter run, step one generated sixteen valid free-autoregressive completions across four current-step source groups, produced non-uniform semantic rewards and group-relative advantages, and changed the LoRA trainable tensors. Step two's sixteen samples all became malformed and its current advantages were zero. The final trained policy tied base on frozen selection (`0.875` accuracy) and untouched test (`0.725` accuracy), so the declared stable rule retained base and a fresh OS BEAM reproduced it byte-for-byte. `examples/local_grpo_banking77/exercised-json-multistep-result.json` preserves this immutable neutral result. It establishes ordinary model-generated semantic-signal weight-change mechanics, not useful learned behavior, a GRPO win/loss, general effectiveness, parity, reliability, or BEAM superiority.

Two owning product facades exposed by that run are repaired without reinterpreting it. Local `train_kwargs` now honor only allowlisted official TRL configuration (`learning_rate`, `beta`, `loss_type`, and `scale_rewards`), normalize and reject invalid values before worker/model startup, create a session-owned runtime contract, and bind exact content into protocol and durable resume identity. Arbitrary Python kwargs, CISPO substitution, device/model/LoRA changes, and filesystem/executable control remain unavailable. Separately, `checkpoint_selection: :best_validation` now predeclares greedy validation-only selection over trained checkpoints, maximizes finite scalar scores, keeps the earliest checkpoint on ties, persists the ordered history and winner across resume, content-verifies the selected historical artifact, and retains the final trainer state separately. A no-model two-step public lifecycle selected step one over a worse step two and rebound the standalone verified artifact. The frozen JSON run was not retrospectively selected.

The major open GRPO obligation is still useful ordinary learning: a separately named treatment must use the corrected public adapter/configuration/selection path and show honest selection/test behavior without result-driven tuning. Crash recovery after an accepted mutation remains fail-closed when the remote worker cannot prove a durable receipt. Full mmGRPO parity and general effectiveness also remain open.

The separately named documented-defaults treatment then executed exactly once with the same frozen model, rows, seed, JSON adapter, two-step/four-group/four-rollout schedule, prompt, token envelope, and metric. Its sealed runtime contract proves `learning_rate: 1.0e-6`, DAPO loss, group reward scaling, and beta zero were applied. Step one reproduced non-uniform semantic rewards/advantages and changed LoRA tensors; step two again emitted sixteen malformed samples. The selected step-one artifact later tied base at `0.875` selection accuracy and `0.725` untouched-test accuracy with zero parse errors, so the ordinary outer rule retained base and fresh-OS output was byte-identical.

The run also falsified its internal checkpoint measurement. Both in-session scores were `0.0` because the worker used the policy object immediately after `GRPOTrainer.train()` without switching it out of training mode; the same saved step-one bytes behaved normally after inference-mode reload. `exercised-json-defaults-result.json` therefore marks internal selection invalid and does not promote a usefulness claim. The worker now enters eval mode around both sampled and greedy generation and restores the prior trainer mode afterward, with a no-model Python boundary regression. The completed treatment remains unchanged and was not rerun.

**2026-07-26T13:00:00Z**

SIMBA now exposes the exact named instruction/demo parameter snapshot for every validation finalist in its ordinary optimizer report, so a consumer can inspect what was actually selected rather than inferring mutation from candidate counts. A new local Banking77 front door binds the retained fused Qwen task artifact, local Llama reflection, frozen 16/8/40 train/selection/untouched splits, one-attempt transports, parameter-only artifact application, and fresh-OS reproduction with explicit anti-substitution checks.

Its first frozen rule-only execution stopped before untouched evaluation. Eight real sampled task trajectories agreed within each example; the resulting buckets had no eligible better/worse contrast, no reflection call occurred, and SIMBA correctly produced zero candidates. The runner refused to present baseline-only completion as a mutation lifecycle. `examples/local_simba_banking77/exercised-rule-only-stopped-result.json` preserves the exact negative boundary. This is operational truth, not SIMBA mutation, lift, effectiveness, parity, or BEAM-superiority evidence. The ordinary natural-model SIMBA mutation/artifact/held-out obligation remains open.

The ordinary full-strategy continuation closed that operational obligation without rerunning the stopped rule-only search. Under the same fused task artifact, rows, seed, and one-attempt runtime, SIMBA sampled eight trajectories and appended one successful-trajectory demonstration. The mutated program was observed in twelve fused task calls, improved the four-row search batch from `0.50` to `0.75`, then regressed on the separate eight-row validation set from baseline `0.625` to `0.375`. SIMBA therefore retained baseline before the untouched test was opened. Baseline and selected both scored `0.475` accuracy, `0.3333333333` macro-F1, and zero errors on forty untouched rows; the selected checksummed parameter artifact applied to a trusted fresh program and reproduced ordered predictions/errors byte-for-byte in a fresh OS BEAM.

Two ordinary restart/observer defects surfaced and were repaired without repeating optimization calls. The acceptance recorder now counts demo-only finalists as mutations and accepts a rendered demonstration candidate without falsely requiring a reflection transport. Separately, a fresh BEAM can load a known public optimizer's report metadata by loading only the explicit allowlisted owning optimizer module before existing-atom decoding; it still creates no atoms dynamically and arbitrary task field atoms remain owned by the reconstructed trusted program. `examples/local_simba_banking77/exercised-result.json` preserves the exact completed boundary. This establishes one natural-model SIMBA mutation/evaluation/validation/artifact lifecycle and an honest overfit rejection, not general SIMBA effectiveness, positive lift, full DSPy parity, or BEAM superiority.
