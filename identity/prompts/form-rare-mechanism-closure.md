# Rare Form Mechanism Closure

Generate exactly 10 raw identity observations for the pre-release Elixir/OTP
library described below. This is a coverage exercise, not a shortlist. Return
every observation, including awkward or hostile probes. Do not rank, repair,
deduplicate, availability-check, or select anything.

The product truth is narrow: it declares callable language-model programs,
executes them through explicit model/tool boundaries, evaluates outputs, and
optimizes program artifacts under measurable objectives. It does not currently
provide formal proof, universal safety, a protocol, a hosted service, a
community institution, or legal authority.

## Exact Cell Contract

- Exactly 10 candidates with contiguous ordinals 1 through 10.
- Exactly 2 wildcards; the other 8 are non-wildcards.
- Exactly 7 non-wildcards must be genuine ideophones: coined forms whose sound
  directly evokes an action, texture, rhythm, transition, or bounded system
  behavior. Merely sounding playful does not qualify.
- At least 2 of those 7 must also be genuine reduplicated ideophones. State
  both mechanisms explicitly.
- The eighth non-wildcard is a contrast/control that explains why it is not an
  ideophone or reduplication.
- Every candidate uses both territories `abstract-sound-symbolic` and
  `play-games` and records the mechanism in notes.
- Use only atlas strategy IDs. Prefer `abstract-sound-brand`,
  `coined-category-noun`, `ordinary-object`, and `ordinary-verb` where true.
- Include `adjacent-language-ecosystems`, `international-users`, and
  `elixir-newcomers` among the audience lenses across the cell.
- Exactly 2 wildcards overall. Wildcards do not count toward mechanism floors.

## Required Candidate Evidence

For every observation provide pronunciation, intended grammar, coined/no-source
etymology, truthful current and horizon embodiments, audience and architecture
lenses, all six code projections or an exact reason they are unavailable,
speech/dictation and screen-reader risk, actor/action/access/cost/refusal notes,
and limitations. Do not claim listener, native-speaker, accessibility-user,
legal, package, domain, trademark, or marketplace validation.

Use notes with machine-readable prefixes:

- `challenge_cell:rare-sound-closure`
- `mechanism_subtype:`
- `ideophone:true|false`
- `reduplication:true|false`
- `stance:affirmative|ambivalent|adverse`
- `scope:current|horizon|both`
- `code_projections:`
- `accessibility:`
- `power_refusal_cost:`
- `limitation:`

Use a 5 affirmative / 3 ambivalent / 2 adverse stance distribution and a
4 current / 4 horizon / 2 both scope distribution. Surface forms must be
newly generated from this brief; no existing candidate list is supplied.
